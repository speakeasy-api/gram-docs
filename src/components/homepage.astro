---
import GatewayDiagram from "./diagrams/GatewayDiagram.astro";
import CircuitDiagram from "./diagrams/CircuitDiagram.astro";
import ServiceDiscovery from "./diagrams/ServiceDiscovery.astro";
import EnterpriseControls from "./diagrams/EnterpriseControls.astro";
import AgentPortal from "./diagrams/AgentPortal.astro";
---

<div class="homepage">
  <div class="hero-section">
    <h1 class="hero-title">LLM Gateway, Enterprise Ready</h1>
    <p class="hero-description">
      Speakeasy's next-generation API gateway, built for accelerating your AI
      transition at enterprise
    </p>
    <div class="hero-actions">
      <a href="/overview/introduction" class="btn btn-primary">Get started</a>
      <a href="https://app.getgram.ai" class="btn btn-secondary"
        >Join the waitlist</a
      >
    </div>
  </div>

  <section class="hero-section-alt mcp-section">
    <div class="hero-content">
      <h2>Easiest way to host MCP at scale</h2>
      <p class="section-description">
        Enterprise APIs require an enterprise-level experience
      </p>

      <GatewayDiagram />

      <div class="feature-cards">
        <div class="feature-card">
          <div class="audit-log-animation">
            <div class="audit-header">
              <span class="audit-title">🔍 Audit Log</span>
              <span class="live-indicator">LIVE</span>
            </div>
            <div class="audit-entries">
              <div class="audit-entry" data-entry="1">
                <span class="timestamp">14:32:15</span>
                <span class="action">MCP server started</span>
                <span class="status">✓</span>
              </div>
              <div class="audit-entry" data-entry="2">
                <span class="timestamp">14:32:18</span>
                <span class="action">Tool request processed</span>
                <span class="status">→</span>
              </div>
              <div class="audit-entry" data-entry="3">
                <span class="timestamp">14:32:22</span>
                <span class="action">Rate limit threshold</span>
                <span class="status">⚠</span>
              </div>
              <div class="audit-entry" data-entry="4">
                <span class="timestamp">14:32:25</span>
                <span class="action">API response cached</span>
                <span class="status">✓</span>
              </div>
              <div class="audit-entry" data-entry="5">
                <span class="timestamp">14:32:28</span>
                <span class="action">Health check passed</span>
                <span class="status">✓</span>
              </div>
              <div class="audit-entry" data-entry="6">
                <span class="timestamp">14:32:31</span>
                <span class="action">New connection opened</span>
                <span class="status">→</span>
              </div>
            </div>
          </div>
          <h4>Real-time monitoring</h4>
          <p>
            Track MCP performance, response times, and system health with live
            dashboards and alerts
          </p>
        </div>
        <div class="feature-card">
          <div class="analytics-chart">
            <div class="chart-header">
              <span class="chart-title">📊 Performance Metrics</span>
            </div>
            <div class="chart-container">
              <div class="chart-bars">
                <div class="bar bar-1"></div>
                <div class="bar bar-2"></div>
                <div class="bar bar-3"></div>
                <div class="bar bar-4"></div>
                <div class="bar bar-5"></div>
                <div class="bar bar-6"></div>
              </div>
              <div class="chart-labels">
                <span>Mon</span>
                <span>Tue</span>
                <span>Wed</span>
                <span>Thu</span>
                <span>Fri</span>
                <span>Sat</span>
              </div>
            </div>
          </div>
          <h4>Performance analytics</h4>
          <p>
            Deep insights into tool usage patterns, optimization opportunities,
            and ROI metrics
          </p>
        </div>
        <div class="feature-card">
          <div class="deployment-animation">
            <div class="deployment-header">
              <span class="deployment-title">⚡ Deployment Status</span>
            </div>
            <div class="deployment-steps">
              <div class="step completed">
                <div class="step-indicator">✓</div>
                <span class="step-text">Build successful</span>
              </div>
              <div class="step completed">
                <div class="step-indicator">✓</div>
                <span class="step-text">Tests passed</span>
              </div>
              <div class="step deploying">
                <div class="step-indicator spinner">⟳</div>
                <span class="step-text">Deploying...</span>
              </div>
              <div class="step pending">
                <div class="step-indicator">○</div>
                <span class="step-text">Go live</span>
              </div>
            </div>
          </div>
          <h4>Rapid deployment</h4>
          <p>
            One-click deployment with auto-scaling, load balancing, and
            zero-downtime updates
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="hero-section-alt llm-section">
    <div class="hero-content">
      <h2>Unified access to LLMs at enterprise</h2>

      <div class="circuit-section">
        <div class="circuit-description">
          <h3>One API to rule them all</h3>
          <p>
            Access all major models through a single, unified interface. OpenAI
            SDK works out of the box—no code changes required to switch between
            OpenAI, Anthropic, DeepSeek, Llama, and more.
          </p>
          <p>
            The gateway automatically handles failover, load balancing, and cost
            optimization across providers, ensuring your applications always get
            the best performance and reliability.
          </p>
        </div>
        <div class="circuit-diagram-container">
          <CircuitDiagram />
        </div>
      </div>

      <div class="circuit-section">
        <div class="circuit-description">
          <h3>🔀 One API to find them</h3>
          <p>
            Automatic Service Discovery and Evolution through Speakeasy OpenAPI
            Platform. The gateway dynamically discovers and integrates new LLM
            providers, automatically updating your available models without code
            changes.
          </p>
          <p>
            When new providers emerge or existing ones add capabilities, the
            platform automatically detects and incorporates them into your
            unified interface—no manual configuration required.
          </p>
        </div>
        <div class="circuit-diagram-container">
          <ServiceDiscovery />
        </div>
      </div>

      <div class="circuit-section">
        <div class="circuit-description">
          <h3>🔒 One API to bring them all and in the darkness bind them</h3>
          <p>
            Enterprise controls with comprehensive audit trails. Monitor every
            request, enforce usage policies, and maintain complete observability
            across all your LLM interactions.
          </p>
          <p>
            Set rate limits, content filters, and access controls while
            capturing detailed logs of every interaction for compliance,
            debugging, and optimization.
          </p>
        </div>
        <div class="circuit-diagram-container">
          <EnterpriseControls />
        </div>
      </div>
    </div>
  </section>

  <section class="hero-section-alt api-section">
    <div class="hero-content">
      <h2>Curate higher order tools directly from your API</h2>
      <p class="section-description">
        Transform your existing APIs into intelligent tools that AI can use
        seamlessly
      </p>
      <div class="feature-list">
        <div class="feature-item">🛠️ Auto-generate tool definitions</div>
        <div class="feature-item">🔗 Direct API integration</div>
        <div class="feature-item">🎯 Smart parameter mapping</div>
      </div>
    </div>
  </section>

  <section class="hero-section-alt agent-section">
    <div class="hero-content">
      <h2>Agent Portal</h2>
      <p class="section-description">
        Bring LLMs into your workflow, wherever you are.
      </p>

      <AgentPortal />
    </div>
  </section>
</div>

<style is:global>
  main > div:first-child {
    display: none !important;
  }

  main > div {
    border-top: none !important;
  }
  footer {
    display: none !important;
  }
</style>

<style>
  .homepage {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  .hero-section {
    text-align: center;
    padding: 4rem 0;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 300;
    margin-bottom: 1rem;
  }

  .hero-description {
    font-size: 1.25rem;
    color: var(--color-neutral-600);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s;
  }

  .btn-primary {
    background: var(--color-blue-500);
    color: white;
  }

  .btn-primary:hover {
    background: var(--color-blue-600);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-blue-500);
    border: 2px solid var(--color-blue-500);
  }

  .btn-secondary:hover {
    background: var(--color-blue-50);
  }

  .hero-section-alt {
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    padding: 4rem 0;
    margin-bottom: 2rem;
  }

  .hero-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero-section-alt h2 {
    font-size: 2.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    text-align: center;
    color: inherit;
  }

  .section-description {
    font-size: 1.25rem;
    text-align: center;
    margin-bottom: 2rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
    color: inherit;
  }

  .feature-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .feature-item {
    display: flex;
    align-items: center;
    font-size: 1.1rem;
    padding: 0.75rem;
    border-radius: 0.5rem;
    line-height: 1.4;
  }

  .feature-subtitle {
    font-size: 0.85rem;
    opacity: 0.8;
    font-weight: 400;
    margin-top: 0.25rem;
    display: block;
    line-height: 1.3;
  }

  .feature-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
    align-items: stretch;
  }

  .feature-card {
    margin-top: 0;
    padding: 2rem;
    border-radius: 1rem;
    text-align: center;
    transition: transform 0.2s ease;
    display: flex;
    flex-direction: column;
    height: 100%;
    justify-content: flex-start;
    align-items: center;
  }

  .feature-card:hover {
    transform: translateY(-4px);
  }

  .feature-icon {
    font-size: 3rem;
    margin: 0 0 1rem 0;
    display: block;
  }

  .feature-card h4 {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 0 0 0.75rem 0;
    color: inherit;
  }

  .feature-card p {
    font-size: 0.95rem;
    line-height: 1.5;
    color: inherit;
    opacity: 0.9;
    margin: 0;
  }

  /* Audit Log Animation */
  .audit-log-animation {
    background: rgba(0, 0, 0, 0.05);
    border-radius: 0.5rem;
    padding: 1rem;
    margin-bottom: 1rem;
    width: 100%;
    font-family: monospace;
    font-size: 0.75rem;
  }

  .audit-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }

  .audit-title {
    font-weight: 600;
  }

  .live-indicator {
    background: #ef4444;
    color: white;
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.625rem;
    font-weight: 600;
    animation: pulse 2s infinite;
  }

  .audit-entries {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    height: 120px;
    overflow: hidden;
    position: relative;
  }

  .audit-entry {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.5rem;
    align-items: center;
    padding: 0.25rem;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 0.25rem;
    background: rgba(255, 255, 255, 0.5);
    opacity: 0;
    animation: logTail 12s ease-in-out infinite;
    transform: translateY(100px);
  }

  .audit-entry[data-entry="1"] {
    animation-delay: 0s;
  }
  .audit-entry[data-entry="2"] {
    animation-delay: 2s;
  }
  .audit-entry[data-entry="3"] {
    animation-delay: 4s;
  }
  .audit-entry[data-entry="4"] {
    animation-delay: 6s;
  }
  .audit-entry[data-entry="5"] {
    animation-delay: 8s;
  }
  .audit-entry[data-entry="6"] {
    animation-delay: 10s;
  }

  .timestamp {
    color: #6b7280;
    font-size: 0.625rem;
  }

  .action {
    font-size: 0.6875rem;
  }

  .status {
    font-weight: 600;
  }

  /* Analytics Chart Animation */
  .analytics-chart {
    background: rgba(0, 0, 0, 0.05);
    border-radius: 0.5rem;
    padding: 1rem;
    margin-bottom: 1rem;
    width: 100%;
  }

  .chart-header {
    margin-bottom: 1rem;
    text-align: center;
  }

  .chart-title {
    font-weight: 600;
    font-size: 0.875rem;
  }

  .chart-container {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .chart-bars {
    display: flex;
    justify-content: space-between;
    align-items: end;
    height: 80px;
    gap: 0.25rem;
  }

  .bar {
    background: linear-gradient(to top, #3b82f6, #60a5fa);
    border-radius: 0.125rem 0.125rem 0 0;
    width: 100%;
    animation: growBar 2s ease-out forwards;
    transform-origin: bottom;
    transform: scaleY(0);
  }

  .bar-1 {
    animation-delay: 0.2s;
    height: 40%;
  }
  .bar-2 {
    animation-delay: 0.4s;
    height: 65%;
  }
  .bar-3 {
    animation-delay: 0.6s;
    height: 50%;
  }
  .bar-4 {
    animation-delay: 0.8s;
    height: 80%;
  }
  .bar-5 {
    animation-delay: 1s;
    height: 90%;
  }
  .bar-6 {
    animation-delay: 1.2s;
    height: 75%;
  }

  .chart-labels {
    display: flex;
    justify-content: space-between;
    font-size: 0.625rem;
    color: #6b7280;
  }

  /* Deployment Animation */
  .deployment-animation {
    background: rgba(0, 0, 0, 0.05);
    border-radius: 0.5rem;
    padding: 1rem;
    margin-bottom: 1rem;
    width: 100%;
  }

  .deployment-header {
    margin-bottom: 1rem;
    text-align: center;
  }

  .deployment-title {
    font-weight: 600;
    font-size: 0.875rem;
  }

  .deployment-steps {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .step {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.75rem;
  }

  .step-indicator {
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .step.completed .step-indicator {
    background: #10b981;
    color: white;
  }

  .step.deploying .step-indicator {
    background: #f59e0b;
    color: white;
  }

  .step.pending .step-indicator {
    background: #d1d5db;
    color: #6b7280;
  }

  .spinner {
    animation: spin 1s linear infinite;
  }

  .step.deploying {
    animation: deploymentProgress 4s ease-in-out infinite;
  }

  /* Keyframe Animations */
  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.7;
    }
  }

  @keyframes logTail {
    0% {
      opacity: 0;
      transform: translateY(100px);
    }
    8.33% {
      opacity: 1;
      transform: translateY(0);
      border-color: rgba(34, 197, 94, 0.4);
      box-shadow: 0 1px 3px rgba(34, 197, 94, 0.2);
    }
    16.66% {
      border-color: rgba(0, 0, 0, 0.1);
      box-shadow: none;
    }
    83.33% {
      opacity: 1;
      transform: translateY(0);
    }
    100% {
      opacity: 0;
      transform: translateY(-30px);
    }
  }

  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateX(-10px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes growBar {
    from {
      transform: scaleY(0);
    }
    to {
      transform: scaleY(1);
    }
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes deploymentProgress {
    0%,
    100% {
      background: rgba(245, 158, 11, 0.1);
    }
    50% {
      background: rgba(245, 158, 11, 0.2);
    }
  }

  /* Circuit Section Layout */
  .circuit-section {
    display: grid;
    grid-template-columns: 1fr 3fr;
    gap: 2rem;
    align-items: center;
    margin: 2rem 0;
  }

  .circuit-description h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: inherit;
  }

  .circuit-description h4 {
    font-size: 1rem;
    font-weight: 600;
    margin: 1.5rem 0 0.5rem 0;
    color: inherit;
    line-height: 1.3;
  }

  .circuit-description p {
    font-size: 0.9rem;
    line-height: 1.5;
    margin-bottom: 0.75rem;
    color: inherit;
    opacity: 0.9;
  }

  .circuit-description p:last-child {
    margin-bottom: 0;
  }

  .circuit-diagram-container {
    display: flex;
    justify-content: center;
    width: 100%;
  }

  .circuit-diagram-container .circuit-diagram {
    width: 100%;
    max-width: none;
  }

  @media (max-width: 768px) {
    .circuit-section {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .circuit-description {
      text-align: center;
    }
  }

  .mcp-section .feature-item,
  .api-section .feature-item {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .llm-section .feature-item,
  .agent-section .feature-item {
    background: rgba(0, 0, 0, 0.05);
    border: 1px solid rgba(0, 0, 0, 0.1);
  }

  .mcp-section .feature-card,
  .api-section .feature-card {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
  }

  .llm-section .feature-card,
  .agent-section .feature-card {
    background: rgba(0, 0, 0, 0.05);
    border: 1px solid rgba(0, 0, 0, 0.1);
  }

  .mcp-section {
    background: #000000;
    color: white;
  }

  .llm-section {
    background: #ffffff;
    color: black;
  }

  .api-section {
    background: #000000;
    color: white;
  }

  .agent-section {
    background: #ffffff;
    color: black;
  }

  :root:where([data-theme="dark"], [data-theme="dark"] *) {
    .hero-description {
      color: var(--color-neutral-400);
    }

    .mcp-section {
      background: #ffffff;
      color: black;
    }

    .llm-section {
      background: #000000;
      color: white;
    }

    .api-section {
      background: #ffffff;
      color: black;
    }

    .agent-section {
      background: #000000;
      color: white;
    }

    .mcp-section .feature-item,
    .api-section .feature-item {
      background: rgba(0, 0, 0, 0.05);
      border: 1px solid rgba(0, 0, 0, 0.1);
    }

    .llm-section .feature-item,
    .agent-section .feature-item {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .mcp-section .feature-card,
    .api-section .feature-card {
      background: rgba(0, 0, 0, 0.05);
      border: 1px solid rgba(0, 0, 0, 0.1);
    }

    .llm-section .feature-card,
    .agent-section .feature-card {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
    }
  }
</style>
